import { atom } from "recoil";

export const email = atom({
    key: "Email",
    default : ""
})

export const password = atom({
    key: "Password",
    default : ""
})

export const username = atom({
    key : "Username",
    default : ""
})